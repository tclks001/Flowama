# Flowama 项目约定

- Flowama 是个人图形学学习项目；优先保证核心原理、数据流和关键取舍能被项目所有者理解、验证与维护。
- 以小而完整、可独立运行的切片推进。每次变更应有可观察结果，并避免无关重构。
- 物理与交互以视觉可信和可控为目标；所有近似、美术化增强及其适用范围须明确记录，不能伪装为严格物理结论。
- 项目所有者负责核心模型、坐标约定、公开接口与发布判断；AI 可协助实现、测试和整理，但不得成为唯一知识来源。
- 工作区只保留当前支持的实现。新方案替代旧方案时，在同一变更中删除旧代码、测试、脚本和文档入口；历史由 Git 保存。
- 实验须有明确目标、成功判据与退出条件。未被采纳的实验只在开发记录中留下简短结论，不保留兼容层或备用实现，除非项目所有者明确要求。
- 测试只保护当前公开契约和高风险回归；不得为已放弃的路径维持测试或兼容性。默认采用满足当前目标的最小实现，不主动扩展架构、测试矩阵或文档。
- 正式名称使用职责和语义，不使用阶段、候选或版本编号。`docs/` 只说明当前系统，`progress/` 记录过程，Git 保存完整历史。
- 验证结论应说明证据层级。编译、自动测试、运行截图/录像和人工观感互不替代。
- 保持仓库可迁移、可复现：不提交机器专属路径、私密配置或无法重建的本地产物。
- 将稳定的设计决定、已知限制和排障经验写入仓库文档，而非仅保留在对话中。
