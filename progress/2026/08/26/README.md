# 2026-08-26

## 完成

- 完整阅读并梳理当前多亮粉沉降与堆积切片：固定 120 Hz 时间步、12 轮顺序位置投影、解析六面盒壁、三维均匀网格宽相、粒子最小距离约束、位置级摩擦、最终位置反推速度，以及调试渲染与确定性验证的边界。
- 明确当前“空间哈希”实现是直接寻址的三维均匀网格，而非关联容器哈希表：格宽为一个粒子直径；`cellHeads_`、`particleNext_` 构成格内索引链；`occupiedCells_` 只清空和遍历非空格；排序后的格序与链表顺序保证当前 CPU 求解的重复性。每轮检查同格及 26 邻格，并只结算每一对一次。
- 分析本地姿态记录 `recorded-20260825-174951.csv` 的前 240 tick 截图。该区间容器姿态恒为单位四元数；身份姿态下局部重力含有负 Y 分量，前深度层约在 tick 20 先压到薄盒侧壁。侧壁位置摩擦使前层先减速，后层经粒子接触追上并剪切为跨深度、跨相邻高度层的规则对角接触链；接触链端部表现为可见的首尾层分离。
- 确认落地后的可见上抛来自同一位置解算流程：首次 Z 底面接触在 tick 134，首次沿重力反向速度在 tick 139。当前位置投影将下层粒子推出底面和彼此，最终 `(position - previousPosition) / dt` 把这些向上修正写入速度；当前无黏性载液拖曳和速度级非弹性接触，因而会出现不符合目标亮粉效果的弹射。
- 明确后续碰撞模型的方向：保留均匀网格、解析盒壁和法向非穿透；将位置稳定化与速度响应拆开，使用零恢复的速度级法向约束和以法向冲量为上限的切向摩擦。真正物理子步不同于当前的 12 次约束迭代，是否引入应在新接触模型与终端速度确定后单独评估。
- 明确验证代码保留原则：当前公开契约和高风险行为的验证应长期保留并随实现演进；一次性私有探针不进入项目。未来应添加并保留墙面斜撞、等质量粒子正碰、单粒子落地、短堆落地和 256 颗粒沉降等小型接触回归场景。

## 验证

- `scripts/verify.ps1 -Ticks 240 -MotionTrack data/motion-tracks/recorded-20260825-174951.csv` 成功完成：256 个粒子状态有效，最大格子占用为 2，最后一轮候选对为 1261、接触对为 267、最大残余穿透为 0.001588。
- 人工检查 `artifacts/captures/recorded-20260825-174951/` 的 tick 44、60、132、156 截图：落地前存在规则层间分离，落地后存在可见的上抛颗粒；该现象已作为当前接触模型限制记录在实现文档中。

## 下一步

- 在新的、独立的碰撞响应切片中替换当前位置级摩擦与“全部位置修正反推速度”的接触响应；完成单体接触验证和当前 256 颗粒 capture 验收后，再进入静止黏性载液拖曳、浮力和更自然的确定性初始分布。
