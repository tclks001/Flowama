# 2026-08-27

## 完成

- 按当前实现的职责重新整理文档入口：`docs/README.md` 现在明确区分姿态轨迹、三维颗粒调试切片和自动化输出的阅读问题，并提供当前源码职责索引。姿态轨迹页只保留 CSV 契约、桌面记录、时间戳输入切分和固定步回放；旋转参考系、局部重力、惯性项、盒壁约束和颗粒接触统一收敛到三维颗粒调试切片；自动化页只保留运行模式、脚本、性能剖析和 BMP 输出。
- 修正三维颗粒调试切片中的预测步骤描述：当前速度积分的是重力、科里奥利、欧拉和离心项组成的总局部加速度，而非仅重力。补充局部静止盒壁与旋转非惯性项的关系，以及无接触自由粒子验证的适用范围。
- 分析桌面录制 `recorded-20260826-170320.csv`：850 个姿态样本中有 671 个连续变化 tick（79.03%）；操作区间为 tick 102 至 772，期间没有零姿态增量空洞。时间戳 Δq 切分消除了旧录制中“保持数 tick 后集中跳变”的输入积压模式。该轨迹仍包含用户高速换轴造成的真实角加速度峰值；这与旧式输入压缩脉冲区分处理，暂不通过历史窗口掩盖。
- 分析桌面录制与截图 `recorded-20260826-171757.csv` 的 tick 436 附近：tick 436 接近短暂停顿，随后的摇晃在 tick 443 和 464 一带产生约 `963` 与 `1027 rad/s²` 的局部角加速度量级。欧拉项 `-alpha x r` 与离心项可显著超过 `9.81 m/s²` 重力，并在局部静止盒壁上形成沿壁面分量；这解释了世界坐标中亮粉被旋转壁面带起时，在容器局部系里表现为上爬的原因。颗粒团的具体轨迹还会受到当前位置投影、摩擦和速度重建的影响。

## 验证

- `scripts/verify.ps1 -Ticks 849 -MotionTrack data/motion-tracks/recorded-20260826-170320.csv` 成功完成：输入时间线终点与空闲区间误差均为 `0.000000 rad`；轨迹峰值角速度为 `30.567 rad/s`、峰值角加速度为 `4505.249 rad/s²`；256 个粒子全程保持有效。
- `scripts/capture.ps1 -Ticks 849 -CaptureEvery 15 -MotionTrack data/motion-tracks/recorded-20260826-170320.csv -OutputDirectory artifacts/captures/recorded-20260826-170320-analysis` 输出 57 张 BMP，用于人工检查高速输入段不再出现“保持后单帧跳变”。
- `scripts/verify.ps1 -Ticks 1200 -MotionTrack data/motion-tracks/recorded-20260826-171757.csv` 成功完成：旋转参考系验证通过；256 个粒子状态有效；该整段轨迹的峰值角速度为 `25.519 rad/s`、峰值角加速度为 `1853.985 rad/s²`。

## 下一步

- 当前输入时间线已将桌面事件时间与 120 Hz 物理时间对齐；如果高速换向的惯性强度经人工观感确认仍过大，应单独设计容器运动控制器的角速度、角加速度或执行器响应约束，而不是重新引入只作用于受力的历史平滑。
- 继续把接触位置投影与速度响应分开处理的时机，留待静态载液拖曳切片与干燥颗粒视觉验收共同决定。
