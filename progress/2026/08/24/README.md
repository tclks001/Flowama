# 2026-08-24

## 完成

- 根据实际拖拽观感反转右键上下拖拽的俯仰映射；左右映射、固定步长和屏幕向下重力规则保持不变。
- 复盘并理解当前最小切片的 OpenGL 上下文、线框/点精灵绘制、SDL 鼠标事件、局部轴角相机旋转、固定步长和解析盒壁接触。
- 新增 docs/three-dimensional-debug-slice.md，保留当前实现的数据流、坐标约定、近似范围和验证方法。
- 将手动运行、固定步验证和固定步截图拆为独立运行驱动；三者共享单步推进与渲染，verify 与 capture 当前均无输入。
- 删除 --automation，新增互斥的 --verify 与 --capture 命令行契约；新增 scripts/capture.ps1，输出 Git 忽略的 artifacts/。
- 截图采用零新依赖的 24-bit BMP：从 OpenGL 后台缓冲读取 RGB，再写为底向上的 BGR BMP，避免 PPM 在当前预览工具中不可直接查看的问题。
- 理解并验收当前验证与截图路径的职责：少量 BMP 用于可重建的关键帧检查；高帧率 BMP 序列不是正式录屏格式，连续视频留待未来作为独立能力实现。
- 新增容器姿态轨迹切片：`ContainerPose` 用位置与“容器局部到世界”的四元数表示；桌面模式默认记录每个固定步边界的最终姿态到 `data/motion-tracks/recorded-时间戳.csv`，自动化直接加载同一文本轨迹。
- 将当前受力从屏幕相对重力改为固定世界重力 `(0, 0, -9.81)`；每步转换为容器局部重力。左键拖拽操控容器，右键拖拽只操控相机；薄盒、粒子和局部重力箭头共用容器模型矩阵渲染。
- 新增 `scripts/new-tilt-track.ps1`，生成静止、平滑倾斜、保持、复位的 720 tick 测试轨迹；新增 `data/motion-tracks/tilt-right.csv` 作为当前自动化轨迹素材。

## 关键发现

- upHint_ 是相机上方参考，不必与前向正交；ScreenUp() 通过叉积正交化得到真实屏幕上方。只有后者的反方向才能保证重力在屏幕上严格向下。
- 当前相机不是固定世界轴的 Euler yaw/pitch。它以当前屏幕上、右轴作增量旋转，因此无传统俯仰极点；现有退化保护在位置与上方参考共同旋转的前提下通常不会触发。
- 运行模式的区别应是时间、输入和输出策略，而不是不同的模拟实现。固定 tick 驱动应直接调用同一个 AdvanceFixedStep；截图仅在约定 tick 额外调用 RenderFrame 和像素写入。
- 1280 x 720 的 24-bit BMP 单帧约为 2.76 MB；在 120 Hz 模拟中每两 tick 截图一次会产生约 166 MB/s 的原始数据，并且每帧同步读回可能拖慢捕获。因此当前 capture 是离线验收工具，不以实时录制为目标。
- 轨迹的 tick `N` 是该固定步边界的容器状态：它先用于该 tick 的渲染，再作为区间 `[N, N+1)` 的容器姿态参与粒子积分。自动化加载轨迹时先直接应用 tick `0`，避免默认容器姿态造成首帧跳变。

## 验证

- 反转映射后，scripts/build.ps1 成功编译，scripts/verify.ps1 -Ticks 120 成功完成。
- 重构后，scripts/verify.ps1 在 120 与 720 tick 下均成功；scripts/capture.ps1 -Ticks 720 -CaptureEvery 120 输出 7 张 1280 x 720 BMP。首尾截图经人工检查：粒子从盒内上方下落到右下边界，图像方向正确。
- `scripts/new-tilt-track.ps1` 生成 0 至 720 的连续样本；`scripts/verify.ps1 -Ticks 720 -MotionTrack data/motion-tracks/tilt-right.csv` 成功完成。使用同一轨迹的截图输出 7 张 BMP，倾斜阶段的盒体旋转、世界向下箭头与粒子向低侧移动经人工检查一致；重复捕获的 tick 240 SHA-256 相同。
- 自动化要求轨迹覆盖 `0` 至指定 `Ticks`；以 721 tick 运行 720 tick 轨迹时在创建图形上下文前以非零状态拒绝。

## 下一步

- 由项目所有者在桌面窗口中实际操作左键容器倾斜，检查生成的时间戳轨迹并用 `capture.ps1 -MotionTrack` 回放验收。确认交互手感后，再决定是否进入静态多层容器表达；连续视频导出应在确有展示需求时作为独立切片实现。
